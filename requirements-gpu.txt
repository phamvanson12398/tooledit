# Thư viện NVIDIA cho nhận dạng thoại trên GPU (Windows, khoảng 1.3 GB).
# faster-whisper / ctranslate2 4.x cần cuBLAS cho CUDA 12 và cuDNN 9 cho CUDA 12.
# app/gpu_libs.py tự thêm thư mục DLL của các gói này vào PATH khi chạy.
nvidia-cublas-cu12; sys_platform == "win32"
nvidia-cudnn-cu12==9.*; sys_platform == "win32"
